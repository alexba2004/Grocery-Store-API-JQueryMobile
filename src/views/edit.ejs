<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://code.jquery.com/mobile/1.4.5/jquery.mobile-1.4.5.min.css" />
    <style>
        /* Estilos para el formulario */
        body {
            background-color: #3498db; /* Color de fondo */
            padding: 20px;
        }

        .form-container {
            background-color: #f1f2f6; /* Color de fondo */
            max-width: 400px;
            margin: 20px auto; /* Margen */
            padding: 20px; /* Espaciado interno */
            border-radius: 10px; /* Bordes redondeados */
        }

        h1 {
            background-color: #2c3e50; /* Color de fondo del título */
            color: #fff; /* Color de texto */
            text-align: center; /* Alineación centrada */
            padding: 10px; /* Espaciado */
            border-radius: 10px; /* Bordes redondeados */
            margin-top: 0; /* Eliminar margen superior */
        }

        label {
            display: block;
            margin-top: 10px;
            font-weight: bold;
        }

        input[type="text"],
        input[type="number"],
        select {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        input[type="date"] {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        .ui-slider-switch {
            width: 60px !important;
        }

        button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
        }

        button:hover {
            background-color: #45a049;
        }
    </style>
    <title>Edit Product</title>
</head>
<body>
    <!-- Contenedor del formulario -->
    <div class="form-container">
        <!-- Título -->
        <h1>Edit Product</h1>
        <!-- Formulario de edición -->
        <form action="/<%=product.barcode%>" method="post" data-theme="b">
            <label for="barcode">Barcode:</label>
            <input type="text" id="barcode" name="barcode" placeholder="Barcode" value="<%= product.barcode %>">
            <label for="brand">Brand:</label>
            <input type="text" id="brand" name="brand" placeholder="Brand" value="<%= product.brand %>">
            <label for="description">Description:</label>
            <input type="text" id="description" name="description" placeholder="Description" value="<%= product.description %>">
            <label for="price">Precio:</label>
            <input type="number" id="price" name="price" placeholder="Precio" value="<%= product.price %>">
            <label for="cost">Costo:</label>
            <input type="number" id="cost" name="cost" placeholder="Costo" value="<%= product.cost %>">
            <label for="stock">Existencias:</label>
            <input type="number" id="stock" name="stock" placeholder="Existencias" value="<%= product.stock %>">
            <label for="expiredDate">Fecha de Expiración:</label>
            <input type="date" id="expiredDate" name="expiredDate" placeholder="Fecha de Expiración" value="<%= product.expiredDate %>">
            <label for="status">Estatus:</label>
            <select name="status" id="status" data-role="slider">
                <option value="0" <%= product.status == 0 ? "selected":"" %> >No</option>
                <option value="1" <%= product.status == 1 ? "selected":"" %> >Si</option>
            </select>
            <button type="submit">Guardar</button>
        </form>
    </div>
</body>
</html>
